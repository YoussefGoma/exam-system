// oop implemented for future use
class Question {
    constructor(title,imageSrc,answers,correctAnswer) {
        this.title = title;
        this.imageSrc = imageSrc;
        this.answers = answers;
        this.correctAnswer = correctAnswer;
    }
}